# Variables d'environnement pour le backend en production

# Environnement
NODE_ENV=production

# Base de données (SQLite en production - migrer vers PostgreSQL recommandé)
DATABASE_URL="file:./prisma/prod.db"

# Secret pour l'authentification admin (CHANGEZ-MOI !)
ADMIN_SECRET="CHANGEZ_MOI_PAR_UN_SECRET_FORT"

# Port du serveur backend
PORT=4000

# Origine CORS (votre domaine de production)
CORS_ORIGIN=https://concert.ligugesocial.club
